# Roadmap - Vale Transporte CAPS

> Fases futuras do projeto. Separa claramente o que está **planejado** do que já foi **implementado**.

## Legenda de Status

- ✅ **Implementado** — entregue.
- 🔜 **Planejado** — fase futura, não iniciada.
- ⏳ **Em andamento** — em execução.

## Estado Atual

- ✅ **Sprint 40 — Relatório Resumo de Vales:** nova aba **Resumo** (primeira) em /dashboard/relatorios, exclusiva do Gestor ativo — cards de totais (pacientes, liberações, vales autorizados, vales retirados, saldo), distribuição contínuas/avulsas e tabela consolidada por paciente ordenada por maior autorizado. Agregação NO SERVIDOR sobre dados existentes (gregarResumo puro + RelatorioRepository.obterResumo + elatorioResumoAction), sem migration/RLS/trigger novos e sem tocar nos fluxos de cadastro/liberação/retirada. SEMÂNTICA DO PERÍODO: AUTORIZADO = liberações com data_inicio no período; RETIRADO = retiradas com data_hora no período; SALDO = autorizado − retirado (derivado, nunca armazenado). 661 testes verdes.

- ✅ **Sprint 38 — Cadastro de pacientes por origem (RN29) — fecha a decisão institucional "quem cadastra pacientes":** nova migration incremental `20260821000001_pacientes_origem.sql` — enum `origem_paciente` (`regular`/`esporadico`), coluna `pacientes.origem NOT NULL DEFAULT 'regular'` (**todos os pacientes existentes viram regular**), `v_pacientes` e `pacientes_com_cpf()` recriadas com a coluna; RLS de INSERT substituída: **`pacientes_insert_regular`** (gestor/autorizador ativos, origem obrigatória `regular`) e **`pacientes_insert_recepcao_esporadico`** (recepcionista ativa, origem obrigatória `esporadico`) — a antiga `pacientes_insert_autorizador` é removida; **RN29 no BANCO**: `fn_liberacoes_before` rejeita liberação não-avulsa para paciente esporádico (demais validações RN01/RN02/RN27/RN23 e cálculo RN13/RN21 preservados). Domínio: `ORIGENS_PACIENTE`/`ROTULO_ORIGEM_PACIENTE`, `origem` nos types de paciente, `validarLiberacao({ origemPaciente })` e `origemPermitidaPorPerfil()` em `regras.ts`; `permissoesPacientes` expõe **`podeCriarRegular`** (gestor/autorizador) e **`podeCriarEsporadico`** (recepcionista) — sem permissão genérica. Actions: `criarPacienteAction` deriva a origem do perfil da SESSÃO (cliente não escolhe; origem indevida → ACESSO_NEGADO); `criarLiberacaoAction` lê a origem do paciente e repassa à validação do service. UI: gestor/autorizador veem **"Novo paciente"**, recepcionista vê **"Paciente Esporádico"**; selo "Esporádico" na listagem; formulário de liberação desabilita Contínua e força Avulsa para paciente esporádico (o banco continua sendo a autoridade). Auditoria: rótulo/formato do campo `origem`. Testes: domínio, actions (✓/✗ por perfil × origem), services, repositories, componentes, navegação e integração real env-guarded (`origemAplicada()`: pulados até a migration ser aplicada no remoto). **Pendência operacional:** aplicar a migration no Supabase remoto (deploy explícito fora do escopo).
- ✅ **Sprint 37 — Estabilização do Auth nos testes (Fase A) + Relatórios Fase 8 (Fase B):** **Fase A — rate limit do Supabase Auth eliminado dos testes de integração:** a causa era **1 signIn por `it()`** (59 signIns/execução, burst paralelo dos workers do Vitest estourando o limite por minuto do Auth) — corrigido com sessão **única por perfil por execução**: `tests/global-setup.ts` autentica 1× (5 signIns sequenciais) e grava em `%TEMP%`; os arquivos de integração reconstroem os clients via `auth.setSession()` (local, sem rede) — **zero signIns nos workers**; retry/backoff **somente** para HTTP 429 (nunca mascara erros reais); testes de concorrência continuam reais (`Promise.allSettled`); **Fase A aceita: 3 execuções consecutivas `npm test` 490/490, 490/490, 490/490** (lint/tsc/build limpos). **Fase B — primeira camada de Relatórios (Fase 8 do ROADMAP, confirmada como próximo item):** `/dashboard/relatorios` **exclusiva do Gestor ativo** (REPORTS.md; acesso de autorizador = **DECISÃO INSTITUCIONAL PENDENTE** — não implementado), com **3 consultas**: **Liberações** (filtros período/tipo/busca por paciente via `v_pacientes` sem CPF; colunas paciente, tipo, quantidade, período, status, autorizador, total retirado), **Retiradas** (período/busca; data/hora, paciente, liberação, quantidade, recepcionista) e **Consolidado** (autorizado vs. entregue por liberação, **saldo derivado no servidor** = quantidade − Σ retiradas); filtros e paginação **no servidor** (PostgREST `eq`/`gte`/`lte` + `range` + `count: exact`, embeds com alias de FK `liberacoes_profissional_autorizador_id_fkey`/`retiradas_recepcionista_id_fkey`), arquitetura domain → repository → service → action → view; `permissoesRelatorios` + `capacidadeDashboard.relatorios` em `regras.ts`, navegação/sidebar/ícone/ações rápidas integradas; **sem migration** (somente SELECTs via RLS existente — sem policy nova), **sem CPF** nos relatórios, sem exportação (PDF/Excel/CSV **só planejado**); **549 testes** (59 novos: domínio/mapeamento/rótulos, service, repository com mock, actions com gate de Gestor, componente com abas/filtros/paginação, integração real 12/12 das consultas PostgREST por perfil) — **3 execuções consecutivas 549/549, 549/549, 549/549**, lint/tsc/build OK, secret scan limpo.
- ✅ **Sprint 36 — Estabilização Banco/RLS/Auth (P1/P2):** sprint de **estabilização**, sem UI/UX (identidade congelada das Sprints 29–35) e sem inventar regra; migrations **somente após reprodução real** e aplicação remota **autorizada explicitamente** (SQL Editor). **Gap 1 reproduzido e corrigido — race condition no saldo de retiradas:** `fn_retiradas_before` lia a liberação sem lock (TOCTOU) — 4 retiradas concorrentes de 1 em liberação de 2 resultavam em soma=3; **migration 20260817000001** adiciona `SELECT ... FOR UPDATE` na liberação dentro do trigger → concorrência agora serializa (2 aceitas, 2 recusadas, soma=2, sem over-subscription). **Gap 2 reproduzido e corrigido — renovação forjável via PostgREST:** a RLS já bloqueava "quem renova" (migration 20260813000002), mas `fn_liberacoes_before` não validava o vínculo — renovação apontando para liberação de OUTRO paciente era aceita; **migration 20260817000002** valida RN23 no banco (mesmo paciente — DATABASE.md constraint 12) → renovação do mesmo paciente permitida, de outro bloqueada. **Matriz de perfis validada via PostgREST direto (scripts/validate-sprint-36.mjs):** RECEPCIONISTA renova; AUTORIZADOR/GESTOR/INATIVO/SEM VÍNCULO/ANON bloqueados (6/6). **Auditoria de login** investigada e **não implementada** (nenhuma camada grava; `usuario_id NOT NULL` impede falha sem sessão; sem INSERT grant) → **DECISÃO INSTITUCIONAL PENDENTE** registrada. **Matriz de transições de status de liberações** confirmada segura por construção (sem policy de UPDATE + `revoke update` — migration 15) com testes negativos para 5 perfis. **490 testes** (447 passando / 43 falhas **todas** Auth 429 categoria C/ambiental — estabilidade **9/9 verde** isoladamente), lint/tsc/build OK, secret scan limpo; sem UI/Auth/RLS/domínio alterados além das 2 migrations + mapeamento RN23 em `app-error.ts`.
- ✅ **Sprint 24 — Identidade visual premium (visual-only):** nova identidade institucional aplicada a toda a UI **sem tocar em banco/migrations/RLS/triggers/funções/repositories/services/domain/permissões/Auth/primeiro acesso/Server Actions/regras** — paleta oficial definida em `app/globals.css` (`@theme` Tailwind v4): **brand** (azul institucional profundo, 11 tons, para marca/títulos/navegação/ênfase), **accent** (turquesa, 11 tons, para links/destaques/indicadores/nav ativa), **success** (verde, ações principais e sucesso) e neutros muito claros (fundo `#f7f9fb`, cartões brancos, bordas sutis) — **dark mode removido** (media query `prefers-color-scheme` que trocava `--background/--foreground` e interferia na identidade única; sprint não implementa dark mode); `visual-tokens.ts` ampliado (`BOTAO_PERIGO`, `BOTAO_FANTASMA`, `FOCO`, `TITULO_PAGINA`, `SUBTITULO_PAGINA`, `BADGE_SUCESSO/NEUTRO/AVISO/ERRO`) com botão primário verde, secundário neutro, links turquesa, foco `brand-600`; aplicado em Landing, Login, primeiro acesso, Dashboard (shell, sidebar, drawer, breadcrumb, cards, ações rápidas, visão da conta), jornadas Pacientes/Liberações/Retiradas (títulos `brand-900`, steppers `brand-600`, seletores, badges), Usuários e Auditoria (padronizados com `PageHeader`/`EstadoVazio`/`FeedbackErro` e tokens — corrige inconsistências detectadas na auditoria), status badges unificados nos três módulos, `error.tsx` dos 5 módulos padronizados (`BOTAO_PRIMARIO`), `.gitignore` ignora `supabase/.temp/`; **sem imports/bibliotecas novas, sem snapshots frágeis**; **464 testes passando** (restante = `AuthApiError: Request rate limit reached` 429 do Supabase Auth, ambiental como no Sprint 23 — nenhuma falha de aplicação), lint/tsc/build OK, bundle CSS confirma geração das utilities `brand`/`accent`, secret scan limpo. **Nenhuma alteração fora da camada visual.**
- ✅ **Sprint 21 — Consulta de Auditoria pela gestão:** `/dashboard/auditoria` — consulta de **leitura** de `auditoria_logs` (append-only, RLS `auditoria_select_gestor`) exclusiva do **Gestor ativo**; filtros (ação, entidade, responsável, de/até) e paginação **no servidor** (PostgREST `eq`/`gte`/`lte`); tabela desktop + cards mobile, contador, links de paginação que preservam os filtros; modal acessível de detalhes com pares **Campo/Antes/Depois** formatados (enums/booleano/datas pt-BR); `permissoesAuditoria` + `capacidadeDashboard.auditoria` em `regras.ts`; `listarAuditoriaAction` no padrão `exigirUsuarioAtivo()` + gate de Gestor; rótulos/formatação **puros** em `lib/domain/auditoria/labels.ts` com **CPF excluído por defesa em profundidade**; Dashboard/Shell promovem Auditoria a **link/card reais** e `MODULOS_PLANEJADOS` ficou vazio (seção "Em desenvolvimento" oculta quando vazia); **436 testes** não-auth passando + lint/tsc OK; **nenhuma** migration/RLS/trigger/banco alterada (auditoria segue append-only e legível só pelo Gestor).
- ✅ **Sprint 20 — Página operacional de Retiradas (UX/UI da recepção):** `/dashboard/retiradas` com dados reais — listagem mobile-first (tabela desktop + cards mobile) via read-model que embute paciente (sem CPF), liberação e responsável (best-effort conforme RLS; responsável só o gestor vê); **registro em 4 etapas** (Paciente → Liberação → Quantidade → Revisão) com **saldo calculado no cliente** (`liberacao.quantidade − Σ retiradas`, dados reais via `listarLiberacoesAction`/`listarRetiradasAction`) e quantidade limitada ao disponível; identidade (`recepcionista_id`) e `data_hora` **100% server-side** (trigger RN28 — o cliente nunca envia); CTA de registro só para recepcionista ativa; `permissoesRetiradas` + `capacidadeDashboard.retiradas` em `regras.ts`; actions endurecidas com `exigirUsuarioAtivo()` + gate de perfil (padrão de liberações); Dashboard/Shell promovem Retiradas a **link real** e `MODULOS_PLANEJADOS = ["Auditoria"]`; **379 testes** não-auth passando + **integração de retiradas 6/6** no banco real (auditoria `retirada.registrada`, gestor lista com FKs, gestor/autorizador/inativo bloqueados, saldo garantido pelo trigger; comportamento trigger-before-RLS documentado); lint/tsc/build OK; **nenhuma** migration/RLS/trigger/banco alterada (retiradas segue append-only).
- ✅ **Sprint 19 — Redesign UX/UI da jornada de liberações:** formulário em **4 etapas** (Paciente → Tipo e quantidade → Período → Revisão) com stepper e validação por passo (só avança com o passo completo); passo **Revisão** com resumo e **Voltar** preservando dados; renovação com fluxo **distinto** (resumo somente-leitura + banner Renovação; cliente envia apenas `{ renovacaoDeId }`); submissão lendo o estado controlado (DOM mais robusto, mesmo comportamento); tokens visuais (`BOTAO_PRIMARIO/SECUNDARIO`, `INPUT`, `ROTULO`, `CARTAO`, `CONTAINER`, `LINK`) na listagem e no formulário; **contador de resultados** e **banner de sucesso pós-salvar** (5s) na listagem; testes de componentes atualizados/ampliados (+4: etapas, validação por passo, voltar preserva dados, contador, feedback pós-criação) e **7º teste de integração** provando que o **autorizador NÃO renova** no banco real — valida a migration **20260813000002** (aplicada pelo usuário), que fecha o **gap de RLS da Sprint 18** (`liberacoes_insert_autorizador` agora exige `renovacao_de_id is null`; renovação vive 100% no banco); lint/tsc/build OK; **nenhuma** migration/service/repository/action/regra/banco alterada nesta sprint.
- ✅ **Sprint 18 — Página operacional de Liberações + correção real de auditoria:** `/dashboard/liberacoes` com dados reais (tabela desktop + cards mobile, busca por paciente/Gestor SUS no servidor via `v_pacientes` sem CPF, lista da recepção só com ativas, badge de status); **criação** pelo profissional autorizador ativo (identidade via `public.usuario_atual_id()` — cliente nunca envia `profissional_autorizador_id`) e **renovação** pela recepção ativa (cliente envia apenas `renovacaoDeId`; servidor localiza a original e preserva autorizador/parâmetros); **migration 20** corrige bug real do `fn_auditoria` (`record new has no field auth_user_id`) que impedia INSERTs em `pacientes`/`liberacoes`/`retiradas` — aplicada e validada (seed + 6 testes de integração); action refatorada com união `CriarLiberacaoDados`, mensagens de erro seguras (mapeamento `23503`); **327 testes** não-auth + integração de liberações 6/6, lint/tsc/build OK; **gap de RLS registrado** (`liberacoes_insert_autorizador` não exigia `renovacao_de_id is null`) — **fechado na Sprint 19** pela migration 20260813000002.
- ✅ **Sprint 00 — Documentação inicial:** visão, domínio, arquitetura, banco, segurança, auditoria, relatórios e roadmap criados em `docs/`.
- ✅ **Sprint 01 — Fechamento de regras de negócio:** regras RN13–RN20 formalizadas, perfis mínimos e matriz de permissões definidos, decisões categorizadas (REGRA DEFINIDA / DECISÃO TÉCNICA / DECISÃO INSTITUCIONAL PENDENTE).
- ✅ **Sprint 01.1 — Validação das decisões institucionais críticas:** seção "DECISÕES INSTITUCIONAIS CRÍTICAS" criada em `DOMAIN.md`, com situação atual, decisão necessária, impacto, opções e indicação de bloqueio para a modelagem (itens 1–4, 8, 10–12 bloqueiam o schema; 5–7, 9 podem ficar para depois).
- ✅ **Sprint 02 — Fechamento das decisões institucionais:** os 8 itens bloqueadores foram transformados em perguntas objetivas em `DOMAIN.md` (contexto, pergunta, alternativas, "Outra regra", observação institucional), marcados como **PENDENTE DE VALIDAÇÃO** para resposta da gestão/equipe do CAPS 3.
- ✅ **Sprint 03 — Consolidação das regras e modelagem conceitual:** decisões institucionais validadas incorporadas (RN21–RN28): liberação contínua por período de 1/3/6 meses, avulsa com 1 dia, sem saldo acumulado, renovação pela recepção, retirada somente pelo paciente, Gestor SUS + CPF, perfis do MVP (Auditor fora), profissional ativo, auditoria com distinção de papéis. **Modelagem conceitual definitiva** documentada em `DATABASE.md`.
- ✅ **Sprint 04 — Modelagem lógica do banco:** especificação lógica detalhada em `DATABASE.md` (tabelas, campos, tipos, PK/FK, constraints, índices, status, datas); **matriz RLS conceitual** em `SECURITY.md`; **eventos de auditoria obrigatórios** com identificadores canônicos em `AUDIT.md`. Nenhum SQL/migration/tabela criado.
- ✅ **Sprint 05 — Revisão final do modelo e regras de retirada:** resolvido `pode_autorizar` (removido/derivado), CPF opcional, três papéis da renovação (autorizou / autoriza nova / registrou na recepção), retiradas (total por soma, quantidade restante por liberação, sem saldo acumulado — RN22), múltiplas liberações sem constraint de unicidade, auditoria com distinção de papéis, hash de integridade como decisão técnica futura, RLS com menor privilégio (CPF só Gestor; dados administrativos restritos). Pendências institucionais: parcelamento, quantidade da retirada, múltiplas liberações, procedimento com profissional inativo na renovação. **Nenhum SQL/migration/tabela criado.**
- ✅ Projeto Next.js (TypeScript, App Router) inicializado via `create-next-app`.
- ✅ **Sprint 06 — Integração Supabase Auth:** `.env.local` configurado; clientes `@supabase/ssr` (browser/server/middleware) criados; `proxy.ts` (Next 16) protegendo `/dashboard`; tela de login mínima com Server Actions (`useActionState`); logout; Vitest 4 instalado/configurado; validação de conexão com o Supabase (integração testada com credenciais inválidas → `400 invalid_credentials`). **Nenhuma tabela/migration/RLS criada.**
- ✅ **Sprint 06.1 — Validação final do Auth:** nomenclatura de chave padronizada para `NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY` (Publishable Key moderna; sem `SERVICE_ROLE_KEY`/anon legada) em `.env.local`, código, testes e docs; reconhecimento de usuário funcional (perfil/status) preparado em `lib/auth/profile.ts` (provisório, via `user_metadata`, até a tabela `usuarios`); **login real validado** (login, sessão, acesso a `/dashboard`, logout, bloqueio pós-logout, redirecionamento de não autenticados); Vitest (9 testes), lint, typecheck e build passando; `.env.local` confirmado no `.gitignore` e nenhuma credencial privilegiada no repositório. **Nenhuma tabela/migration/RLS/CRUD criado.**
- ✅ **Sprint 07 — Schema físico no Supabase (migrations):** 10 migrations criadas em `supabase/migrations/` a partir do modelo lógico (Sprint 05): enums (tipo_liberacao, tipo_evento_auditoria, perfil), `pacientes`, `usuarios`, `liberacoes`, `retiradas`, `auditoria_logs`, funções (`auth_uid`, `auth_perfil`, triggers de auditoria/liberação), `v_pacientes` (sem CPF, leitura autorizador/recepção), `v_liberacoes` com saldo/status, RLS de menor privilégio e grants. Decisões técnicas aplicadas na revisão: somas de quantidades como `bigint`; campos derivados da sessão setados **somente em INSERT** (imutabilidade de auditoria); `v_pacientes` para leitura sem CPF; update em `pacientes` restrito ao Gestor e somente a status; revokes de operações mutáveis em `auditoria_logs` para `authenticated`. **Aplicável ao Supabase via CLI; nenhuma migration aplicada ainda.**
- ✅ **Sprint 08 — Camada de domínio, repositórios, serviços e testes (sem UI):** `lib/domain` (enums canônicos reutilizados por `lib/auth/profile.ts`; tipos de entidade; `app-error` com códigos; `regras` RN01–RN28 e validações RN01–RN05/RN08/RN09); `lib/repositories` (paciente, usuário, liberação, retirada — Postgres); `lib/services` (paciente, usuário, liberação, retirada — validações antes do banco, sem update/delete de liberação/retirada/auditoria); server actions mínimas (`app/actions`); testes unitários (84 passando) e testes de integração RLS/CPF/auditoria/concorrência (env-guarded). **Nenhuma migration aplicada; UI de negócio ainda não iniciada.**
- ✅ **Sprint 11 — Primeira página de negócio: Pacientes:** rota `/dashboard/pacientes` (Server Component) com autenticação real (proxy + `getUser()`), gate por perfil/status via `permissoesPacientes()` (UX; autoridade continua no banco/RLS); listagem via `v_pacientes` (sem CPF) com pesquisa por nome/Gestor SUS (ilike no repositório, termo sanitizado); cadastro (opcional com CPF) e edição de dados pelo **profissional autorizador ativo**; alteração de status pelo **gestor ativo**; recepcionista somente leitura; usuário inativo/sem perfil bloqueado; estados loading/empty/error/sucesso/salvando; `PacienteForm` com `useActionState`; testes de componentes (React Testing Library/jsdom), server actions, serviços e regras; mapeamentos de erro `23505`/`23514`; **sem DELETE**, **sem escrita em auditoria**, **sem SELECT direto de CPF**. **Nenhuma migration alterada.**
- ✅ **Sprint 11.1 — Fechamento da integração real (fonte definitiva de perfil/status):** migrations 01–15 **aplicadas no Supabase** e validadas com usuários reais; removida a dependência de `user_metadata` — `getUsuarioFuncional()` agora resolve perfil/status via RPCs `perfil_atual()`/`usuario_ativo_atual()` (infra existente, sem migration nova; migration 16 avaliada e descartada); seed de usuários de teste separado de migrations (`scripts/seed-test-users.mjs`, via Admin API, sem senhas em banco/commits); migration **17** (revogar SELECT(cpf) — inócua isolada) e **18** (revogar SELECT de tabela + grants por coluna exceto `cpf`) corrigem o acesso real ao CPF; CPF lido **somente** por `pacientes_com_cpf()`.
- ✅ **Sprint 16 — Gestão e criação de usuários Auth (Admin API server-only):** o Gestor passou a **criar o usuário completo** pela UI (`/dashboard/usuarios` → "Novo usuário") sem acesso ao banco — camada `lib/supabase/admin.ts` exclusiva de servidor (guard client-side; `SUPABASE_SERVICE_ROLE_KEY` nunca chega ao navegador/bundle/HTML/logs; `.env.example` com placeholder); fluxo Auth → `public.usuarios` com **UUID real** retornado pela Admin API (`email_confirm: true`), INSERT pelo client do Gestor (RLS `usuarios_insert_gestor` + auditoria) e **compensação em falha parcial** (remove o Auth recém-criado se o vínculo falhar; nunca deixa Auth sem vínculo enganosamente ativo); duplicidade tratada (Auth "já existe uma conta" e `usuarios` "já existe um usuário") com mensagens seguras; **senha temporária forte exibida 1x** ao Gestor (sem SMTP; nada persistido/logado) + `validarCriacaoUsuario` (nome/e-mail/RN02) revalidada no servidor; UI `novo-usuario-form.tsx` (dialog mobile-first acessível, profissão só p/ autorizador, loading/sucesso/erro) + botão [Novo usuário] com lista atualizada após concluir; **Gestor→Gestor permitido** (regra institucional não documentada → **DECISÃO INSTITUCIONAL PENDENTE**, sem inventar RN/RLS nova); **266 testes passando** (48 novos), lint/tsc/build OK, bundle client limpo (grep sem `SERVICE_ROLE`/admin nos chunks), secret scan limpo; **nenhuma migration/RLS/policy nova** — `usuarios_insert_gestor` existente usada; sem DELETE; ativação/inativação preservadas.
- ✅ **Sprint 15 — Dashboard UX premium, profissional e mobile-first:** `/dashboard` deixou de ser a tela funcional básica e virou produto — shell institucional (`components/dashboard/dashboard-shell.tsx`, client só na navegação) com header fixo (marca, perfil, e-mail), **sidebar desktop** (navegação com item ativo via `aria-current`/borda+cor, módulos "Em desenvolvimento" não-link para Liberações/Retiradas/Auditoria, resumo da conta + Sair) e **menu mobile** (`role="dialog"`, fechar por `Esc`/overlay/clique na rota); conteúdo continua Server Component (`dashboard-home.tsx`) com saudação contextual (fallback neutro — nome não disponível via infra atual), chip de perfil (`ROTULO_PERFIL`), ações principais (cards de módulos **reais** por perfil via nova função pura `capacidadeDashboard` em `regras.ts`: todos os ativos → Pacientes; Gestor ativo → + Usuários), visão geral da conta (perfil, situação, e-mail — **sem métricas fictícias**) e estado "Acesso não configurado" para inativo/sem vínculo; `loading.tsx`/`error.tsx` no painel; tokens ampliados coerentemente (`CARTAO`, `CHIP`, `NAV_LINK`, `CONTEINER_PAINEL`); **19 testes novos** (DashboardHome e DashboardShell: saudação/fallback, módulos por perfil, navegação, logout, drawer, sem links inexistentes, sem info sensível) — **218 testes passando**, lint/tsc/build OK; sem migrations/RLS/banco (**proxy.ts, lib/supabase/**, `lib/auth/profile.ts` e autorização intactos).
- ✅ **Sprint 14 — UX do login + primeiro acesso:** página `/login` reformulada para ser continuação natural da Landing (header/footer próprios, pill "Sistema institucional", cartão `rounded-lg` com marca/título/descrição, botão primário e tokens compartilhados via `components/ui/visual-tokens.ts` extraídos da Sprint 13); LoginForm com validação client (vazio, e-mail inválido — associada ao campo via `aria-invalid`/`aria-describedby`), estado de carregamento (`pending` → "Entrando...", botão e campos desabilitados), show/hide de senha, `autocomplete` correto, link de retorno à Landing, `noValidate` + validação própria, mensagens de erro seguras e compreensíveis (`role="alert"`, sem SQL/Supabase/stack); `login()` da action mapeia erros reais (`invalid_credentials`, `email_not_confirmed`, rate-limit) e **corrige redirecionamento aberto em `?next=`** (só rotas internas; externos/`//host` caem em `/dashboard`); redirect normal (`/login` → `/dashboard`), `?next=` (→ `/dashboard/pacientes`) e autenticado saindo de `/login` (→ `/dashboard`) preservados via `proxy.ts` **intocado**; dashboard recebe só o selo "Você entrou no sistema." (transição); 21 testes novos (action de login: validação, erros seguros, redirect interno/externo; LoginForm: render/submit/validação/loading/erro/show-hide/`?next=`/sem info sensível) — **199 testes passando**, lint/tsc/build OK; sem migrations/RLS/infra (**proxy.ts, lib/supabase/** e `lib/auth/profile.ts` intactos**).
- ✅ **Sprint 13 — Landing page / primeira experiência:** rota `/` (boilerplate do Create Next App) substituída por página institucional estática (server component; sem Supabase, auth, fetches ou client state), com header, hero + CTA "Entrar no sistema" → `/login`, seção de módulos (Pacientes, Liberações, Retiradas, Usuários, Auditoria), controle e segurança, fluxo (Paciente → Liberação → Retirada), CTA final e footer; identidade visual em `components/landing/` reutilizando a paleta/camponentes do login/dashboard (zinc, `rounded-md`, botão primário `zinc-900`, `max-w-5xl`); ícones SVG inline (sem imagens de banco); correções de identidade — fonte Geist aplicada no `body` (globals.css) e `lang="pt-BR"`; sem dados institucionais inventados; testes de renderização (178 passando), lint, typecheck e build com `/` **estática**; **nenhuma migration/RLS/tabela nova**.
- ✅ **Sprint 12 — Página de usuários e gestão de perfil/status:** rota `/dashboard/usuarios` (Server Component) restrita ao **Gestor ativo** (`permissoesUsuarios()` espelhando `usuarios_select/update_gestor`); listagem de `public.usuarios` (nome, e-mail, perfil, profissão, status — sem campos internos de auth); pesquisa sanitizada por nome/e-mail (ilike no repositório, mesmo padrão de Pacientes); alteração de status ativo/inativo via `UsuarioService`; perfil **somente leitura** (regra Sprint 08 preservada — repository não muta perfil na UI); criação de `auth.users` documentada como **pendência** (sem Admin API no browser); server actions com autorização explícita de gestor + mensagens seguras (sem vazar SQL); testes de domínio/repositório/serviço/actions/componentes e **integração real RLS de usuários** (gestor lista; autorizador/recepcionista/inativo/sem-vínculo 0 linhas). **Nenhuma migration/RLS alterada.**
- 🔜 **Pendência não-blockante**: teste manual nos 5 cenários no browser (login + navegação) — infraestrutura e dados de teste prontos; execução depende de navegador humano.
- 🔜 **Funcionalidades de negócio ainda sem tela**: relatórios ganharam a **primeira camada** na Sprint 37 (`/dashboard/relatorios` para o Gestor ativo: Liberações, Retiradas e Consolidado com filtros/paginação no servidor); **restam planejados**: consolidações adicionais/"Relatório de Renovações", histórico por paciente e exportação PDF/Excel/CSV (somente planejada), além da **decisão institucional** sobre relatórios para autorizadores.

## Fases Planejadas

### Fase 1 — Modelagem (concluída)
- ✅ Definição de visão, domínio e arquitetura.
- ✅ Fechamento das regras de negócio (RN01–RN28) e matriz de permissões.
- ✅ Respostas da gestão/equipe aos itens bloqueadores, **modelagem conceitual** e **modelagem lógica** (Sprints 03–04).
- ✅ **Revisão final do modelo e regras de retirada (Sprint 05)** — ambiguidades resolvidas; pendências institucionais catalogadas (ver `DATABASE.md`).

### Fase 2 — Autenticação
- ✅ Integração Supabase Auth (Sprint 06).
- ✅ Telas de login/logout mínimas.
- ✅ Proteção de rotas via `proxy.ts` (Next 16).
- ✅ Validação real de usuário autenticado (Sprint 06.1).
- 🔜 Perfis de usuário e autorização por perfil (ver `SECURITY.md`) — preparado em `lib/auth/profile.ts`, aguardando tabela `usuarios` (Fase 3/4).

### Fase 3 — Banco de Dados
- ✅ Migrations de schema criadas em `supabase/migrations/` (Sprint 07): enums, tabelas, funções, views, triggers, RLS e grants.
- 🔜 Aplicação das migrations no Supabase via CLI (`supabase db push`) e validação real.
- 🔜 Políticas de **Row Level Security** — criadas nas migrations; validação pendente de execução real no projeto.
- 🔜 (Com base nas decisões da Fase 1.)

### Fase 4 — Cadastros e Domínio
- ✅ Página de pacientes (`/dashboard/pacientes`) — listagem, pesquisa, cadastro e edição conforme permissões (Sprint 11).
- ✅ Página de usuários (`/dashboard/usuarios`) — gestão de status pelo Gestor ativo (Sprint 12); perfil somente leitura; criação de `auth.users` (Sprint 12).
- ✅ Criação de usuários Auth (Admin API server-only) via UI pelo Gestor ativo (Sprint 16) — senha temporária exibida 1x; troca obrigatória no primeiro acesso em pendência.

### Fase 5 — Liberações
- ✅ Fluxo de liberação por profissional autorizador (Sprint 18) — criação server-side com identidade via `usuario_atual_id()`.
- ✅ Tipos de liberação (contínua / avulsa) e quantidades (1, 2, 4, 8) (Sprint 18).
- ✅ Validações de domínio (RN01–RN05) (Sprint 18).
- ✅ Renovação pela recepção (Sprint 18) — cliente envia só `renovacaoDeId`; autorizador/parâmetros preservados da original. *Gap de RLS fechado na migration 20260813000002 (Sprint 19): `liberacoes_insert_autorizador` exige `renovacao_de_id is null`.*

### Fase 6 — Recepção / Retiradas
- ✅ Fluxo de registro de retirada na recepção (somente o próprio paciente — RN24) (Sprint 20).
- ✅ Controle de quantidade autorizada por liberação (sem saldo acumulado — RN22) — validação em `regras.ts` (RN14) com autoridade final no trigger `fn_retiradas_before` (Sprint 20).
- 🔜 Comprovante de retirada (a confirmar — DECISÃO PENDENTE).

### Fase 7 — Auditoria
- ✅ Geração de logs em todas as ações relevantes (migrations 07/20 — triggers `*_audit`).
- ✅ Imutabilidade técnica dos logs (append-only — grants/RLS; migrations 10/15).
- ✅ Consulta de logs pela gestão (`/dashboard/auditoria` — Sprint 21).

### Fase 8 — Relatórios
- ✅ Primeira camada (Sprint 37): `/dashboard/relatorios` exclusiva do Gestor ativo — **Liberações**, **Retiradas** e **Consolidado** (autorizado vs. entregue por liberação, saldo derivado no servidor) com filtros (período, tipo, paciente via `v_pacientes` sem CPF) e paginação **no servidor**; sem migration/RLS nova; sem exportação.
- 🔜 Consolidações adicionais / "Relatório de Renovações" (REPORTS.md) — planejadas.
- ✅ Histórico por paciente (Sprint 38): linha do tempo da cadeia de liberações/renovações com autorizado/retirado/saldo por evento.
- ✅ Aba Resumo gerencial (Sprint 40): cards de totais (pacientes, liberações, vales autorizados, vales retirados, saldo), distribuição contínuas/avulsas e tabela consolidada por paciente — agregação NO SERVIDOR sobre dados existentes (sem migration/RLS nova). SEMÂNTICA DO PERÍODO: AUTORIZADO = liberações cuja data_inicio está no período; RETIRADO = retiradas cuja data_hora está no período; SALDO = autorizado − retirado (derivado).
- 🔜 Exportação para PDF, Excel e CSV — **somente planejada**.
- 🔜 Acesso de relatórios para autorizadores — **DECISÃO INSTITUCIONAL PENDENTE**.

### Fase 9 — Testes
- ✅ Framework Vitest 4 definido e configurado (Sprint 06).
- ✅ Testes unitários de domínio, serviços e repositórios (Sprint 08; 84 passando).
- ✅ Testes de integração RLS/CPF/auditoria/concorrência criados (env-guarded, requerem projeto de teste dedicado).
- ✅ Testes de fluxo (liberação → retirada) e segurança contra o banco real (Sprint 20 — `retiradas.integration.test.ts` 6/6).

### Fase 10 — Produção / Deploy

### Fase 11 — Primeiro Acesso + UX Premium Mobile-First

- **Objetivo:** fechar o fluxo completo de primeiro acesso (Landing → Login → `/primeiro-acesso` → Troca de senha → Dashboard) e entregar uma experiência premium mobile‑first em todas as telas (Landing, Login, Dashboard, Usuários, Pacientes e `/primeiro-acesso`).
- **Entregas:**
  - Tela `/primeiro-acesso` com validação de senha, feedback de loading, sucesso e erro; layout mobile‑first premium.
  - Segurança reforçada: senha temporária enviada somente ao Supabase Auth; flag `precisa_trocar_senha` limpo via Admin API; nenhuma exposição de senha em URL, logs ou client storage.
  - Proteção de rotas automática: redirects impedem acesso direto ao `/dashboard` enquanto o flag estiver ativo.
  - UX premium: hierarquia visual institucional, touch targets ≥44 px, contraste adequado, foco acessível, sem overflow horizontal.
  - Testes ampliados: +18 novos casos de primeiro acesso; todos os 266 testes da Sprint 16 permanecem passantes; lint, typecheck e build continuam OK.
  - Validações de segurança: senha mínima 8 caracteres, confirmação obrigatória, redirect seguro (`?next=`) protegido contra open‑redirect.
  - Documentação atualizada: roadmap, changelog e security register a estratégia de primeiro acesso, limpeza do flag e comportamento de redirect.
- **Riscos mitigados:** nenhuma alteração em migrations/RLS; SERVICE_ROLE_KEY permanece server‑only; bundle client sem vazamento de credenciais.
- 🔜 Deploy contínuo na Vercel.
- 🔜 Configuração de variáveis de ambiente e domínio.
- 🔜 Monitoramento, observabilidade e hardening de segurança.
- 🔜 Migração de dados legados (se houver) — DECISÃO PENDENTE.

## Dependências Críticas para Avançar

- **Modelagem lógica/física** do banco a partir da modelagem conceitual consolidada (Sprint 03) e **revisão final (Sprint 05)**.
- Resolver **DECISÕES INSTITUCIONAIS PENDENTES** de domínio **não bloqueadoras**: retirada parcelada, quantidade de retirada, múltiplas liberações ativas, comprovante, procedimento com profissional inativo na renovação, cadastro pela recepção, relatórios para autorizadores. Nenhuma bloqueia a criação do schema base — impactam validações de serviço e RLS granular.
- Resolver **DECISÕES INSTITUCIONAIS PENDENTES** de segurança (criação de usuários, MFA, retenção de logs, imutabilidade técnica/hash).

## Sugestão — Sprint 25

> Sprints 11.1–24 concluídas: integração real, página de usuários, Landing, UX do
> login, primeiro acesso, Dashboard de produto, criação de usuários Auth (Admin
> API server-only), página operacional de Liberações, redesign da jornada de
> liberações, página operacional de Retiradas, consulta de Auditoria, UX/UI das
> três jornadas e identidade visual premium (visual-only).

1. **Validar manualmente no browser** a jornada completa com a nova identidade
   (Landing → Login → primeiro acesso → Dashboard → módulos), incluindo menu
   mobile/desktop, contraste das novas cores, foco visível e touch targets.
2. Resolver **DECISÕES INSTITUCIONAIS PENDENTES**: Gestor criar Gestor (Sprint
   16) e itens da recepção (parcelamento, quantidade da retirada, múltiplas
   liberações ativas, comprovante de retirada).
3. **Relatórios** (Fase 8) — primeira camada entregue na Sprint 37 (Liberações/Retiradas/Consolidado p/ Gestor); consolidados adicionais e exportação seguem planejados.

## Nota

- Ordem das fases pode ser ajustada conforme decisões da gestão.
- Nada além da documentação será entregue nesta Sprint.
